type Credentials = {
  phone: string;
  password: string;
};
export default Credentials;

// Это одно и тоже, НЕ УДАЛЯТЬ: НУЖНОЕ - Пригодится когда будем разбирать код
// export default interface Credentials {
//     name: string;
//     password: string;
//   }
